# 本地运行个人网页

## 前置要求

1. **安装 Ruby**（如未安装）：
   ```powershell
   winget install --id=RubyInstallerTeam.RubyWithDevKit.3.4 -e --accept-package-agreements
   ```
   安装完成后**必须重新打开终端**以使 PATH 生效。

2. 验证安装：
   ```powershell
   ruby --version
   bundle --version
   ```

## 启动网站

### 方法一：双击运行（推荐）
直接双击 `run_server.bat`，脚本会自动安装依赖并启动 Jekyll 服务器。

### 方法二：命令行
在项目目录 `ChunlinGong.github.io` 下打开终端，执行：
```powershell
bundle install
bundle exec jekyll serve --livereload
```

### 方法三：Git Bash / WSL
```bash
bash run_server.sh
```

## 访问网站

启动成功后，在浏览器中打开：**http://127.0.0.1:4000**

修改源码后，页面会自动刷新（LiveReload）。
